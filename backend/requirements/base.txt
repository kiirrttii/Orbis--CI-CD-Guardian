# Core dependencies — installed in all environments

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Database — async
sqlalchemy==2.0.30
asyncpg==0.29.0              # async PostgreSQL driver

# Database — sync (Alembic only)
psycopg2-binary==2.9.9       # sync driver for Alembic migrations

# Migrations
alembic==1.13.1

# HTTP client (GitHub integration)
httpx==0.27.0

# Structured logging
structlog==24.2.0

# Utilities
python-dotenv==1.0.1

# ── ML Inference ──────────────────────────────────────────────────────────────
numpy>=1.26,<2
scikit-learn>=1.4,<1.6
xgboost==2.0.3
joblib==1.4.2
shap==0.45.1

# ── Authentication ────────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
